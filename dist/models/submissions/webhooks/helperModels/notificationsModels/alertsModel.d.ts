export declare class AlertsModel {
    category: string;
    code: string;
    title: string;
    message: string;
    helpLink: string;
    severity: string;
    additionalData: string;
    constructor(init?: Partial<AlertsModel>);
}
